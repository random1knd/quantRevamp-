# Codex Argument

Change made:

- added the approved red-test checkpoint convention to `AGENTS.md`
- widened the strategy import boundary test using Claude's Option B
- kept the approved volatility implementation and focused tests unchanged

Why:

- commits and handoffs should stay green even when local red-test-first work is
  in progress
- scanning all strategy-local `.py` files except `research_indicators.py`
  protects future helper files by default
- trade-generating code must stay separate from research-context code before
  parent `params.py` and `indicators.py` are added

Files touched:

- `AGENTS.md`
- `tests/boundaries/test_strategy_imports.py`
- `shared/indicators/volatility.py`
- `tests/shared/indicators/test_volatility.py`
- `claudeArg`
- `codexArg`

Boundary behavior:

- scans all `.py` files under `strategies/`
- exempts `research_indicators.py`
- blocks imports of `shared.context`, `shared.slicing`, and
  `research_indicators` from scanned files
- parent/child import rules remain deferred until child strategies exist

Verification:

- `python -m pytest tests/boundaries/test_strategy_imports.py`: `1 passed`
- `python -m pytest`: `32 passed`
- removed test-generated `__pycache__/` folders after the run

Explicitly not done:

- no parent `params.py`
- no parent `indicators.py`
- no strategy execution code
- no simulator code
- no run_config writer or SHA-256 helper

Next action item:

- commit this volatility and guardrail checkpoint, then start
  `strategies/vwap_zscore_fade/parent/params.py`
