# Codex Argument

Change made:

- Completed Claude's required doc-fix pass after Phase 1 challenge review.
- Updated all 13 requested indicator/inventory docs so they match the approved
  implementation plan and current data constraints.
- No indicator code, strategy code, research-indicator wiring, discovery rerun,
  or slicing was started.

Files changed:

- `docs/indicators/README.md`
- `docs/indicators/price_extension.md`
- `docs/indicators/zscore_family.md`
- `docs/indicators/volatility.md`
- `docs/indicators/regime_statistical.md`
- `docs/indicators/ou_process.md`
- `docs/indicators/kalman.md`
- `docs/indicators/trend_momentum.md`
- `docs/indicators/order_flow.md`
- `docs/indicators/absorption_volume.md`
- `docs/indicators/candle_time.md`
- `docs/indicators/liquidity_toxicity.md`
- `docs/inventories/indicators.md`
- `codexArg`

What changed:

- Added the available post-`prepare_bars` input columns to the indicator README.
- Aligned VWAP docs with existing `session_vwap(frame, price_col, volume_col,
  session_col)` and removed deferred anchor/slot functions from expected builds.
- Added `gap_free_rolling_window` to z-score docs and marked `VolScaledMove`
  blocked.
- Removed percentile ownership from volatility docs and marked `VolRegime` and
  `JumpRatio` blocked.
- Marked Hurst deferred, `RegimeScore` blocked, `ADF_Stationary` threshold
  blocked, and rolling ADF context-only/expensive.
- Added deferred banners to O-U and Kalman docs.
- Removed stale `atr.py` and `momentum.py` references from trend docs; marked
  `MOM_Composite` and `TSMOM` blocked.
- Corrected order-flow docs for `delta(bid_volume, ask_volume)` and labeled
  true OFI not buildable from current bars.
- Consolidated absorption into `volume.py` docs and specified NaN behavior for
  zero price-change absorption denominators.
- Split time-context docs into the planned functions and marked `CandleType`
  blocked.
- Consolidated VPIN approximation into `liquidity.py` docs and specified VPIN
  and Kyle lambda zero-denominator behavior.
- Added feasibility tags to the indicator inventory tables and labeled
  time-bar approximations explicitly.

Verification:

- `.venv\Scripts\python.exe -m pytest` -> 104 passed
- Stale-reference scan found no remaining references to the removed module
  names or deferred expected functions: `atr.py`, `momentum.py`,
  `absorption.py`, `vpin.py`, `session_vwap(bars...)`, `anchored_vwap`,
  `slot_percentile`, `delta(bars)`, `time_context(...)`, `vpin(bars...)`,
  `hurst_estimate`, or `candle_type(...)`.

Confirmation:

- All 13 docs now match the approved plan at the doc level.
- True OFI and true VPIN are documented as not buildable from current time bars.
- Time-bar approximations are labeled context-only where applicable.
- Blocked/deferred items are labeled before any code is written.

Carried forward:

- `_last_same_session_pos` in `strategy.py` is O(N^2). Claude said not to fix
  it during the indicator build; carry this forward until a dedicated cycle.

Stopped before:

- Cycle 1 z-score expansion.
- Any new indicator implementation.
- Research-indicator expansion.
- Discovery rerun or slicing.
