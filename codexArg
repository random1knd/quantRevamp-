# Codex Argument

Change made:

- Completed Cycle 5: added `shared/indicators/order_flow.py`.
- Added `delta`, `cumulative_delta`, `delta_roc`, and `ofi_approx`.
- Did not add any z-scoring or shared indicator imports; callers remain
  responsible for z-scoring flow outputs.
- Did not start Cycle 6 or wire these indicators into any strategy.

New file and functions:

- `shared/indicators/order_flow.py`
- Constants: lines 6-9
- `delta(...)`: lines 12-18
- `cumulative_delta(...)`: lines 21-28
- `delta_roc(...)`: lines 31-41
- `ofi_approx(...)`: lines 44-53
- Local validation helper: lines 56-63

Tests:

- Test file: `tests/shared/indicators/test_order_flow.py`
- Cycle 5 tests added: 11
- Test range: `tests/shared/indicators/test_order_flow.py:12-126`

Verification:

- `.venv\Scripts\python.exe -m pytest tests\shared\indicators\test_order_flow.py`
  -> 11 passed
- `.venv\Scripts\python.exe -m pytest` -> 161 passed

Spec notes:

- `order_flow.py` imports pandas only.
- `cumulative_delta` resets by caller-provided session labels using
  `groupby(session, sort=False).cumsum()` and does not validate session labels.
- `delta_roc` validates positive lookback and returns raw flow difference, not
  a normalized rate.
- `ofi_approx` validates `BidVolume` and `AskVolume`, leaves bar 0 as NaN via
  `.diff()`, includes both required context comments, and is named
  `OFI_Approx`.
- No deviations from the unlocked Cycle 5 scope.

Carried forward:

- `_last_same_session_pos` in `strategy.py` is O(N^2). Claude said not to fix
  it during the indicator build; carry this forward until a dedicated cycle.
- `robust_zscore` is missing an explicit session-reset test. Claude said to add
  it in a cleanup pass after all cycles are done.
- `ma_slope` and `momentum` causality tests are also carried to cleanup.

Stopped before:

- Cycle 6.
- Any strategy or research-indicator wiring.
- Discovery rerun or slicing.
