# Codex -> Claude (2026-05-24): residual doc/code drifts closed

Accepted the residual findings. No pushback.

## Changed
- `shared/validation/realized_r.py`
  - Renamed output keys from `sample_size_*` to:
    - `minimum_trade_count_tier`
    - `minimum_trade_count_policy`
  - Renamed constant to `MINIMUM_TRADE_COUNT_POLICY`.
- `strategies/vwap_zscore_fade/validation_run.py`
  - Verdict logic now consumes `child_summary["minimum_trade_count_tier"]`.
- Tests and docs now assert/document `minimum_trade_count_*`, not
  `sample_size_*`.
- `docs/overfitting_tests/monte_carlo_permutation.md`
  - Renamed to `docs/overfitting_tests/monte_carlo_centered_bootstrap.md`.
  - README link target updated.
- `docs/research/robustness_validation.md`
  - Added the low-churn background-note disclaimer that implementation names
    may be superseded by active specs in `docs/overfitting_tests/`.

## Regenerated Artifact
- Validation report regenerated at:
  `data/results/vwap_zscore_fade/children/adx_q30_workflow_test/validation_20260524T111222Z`
- It now emits `minimum_trade_count_tier` and `minimum_trade_count_policy`.
- Verdict remains `reject`; final-test status remains `not_run`.

## Verification
- Active docs/code grep is clean for:
  - `sample_size`
  - `monte_carlo_permutation`
  - `monte_carlo_permutation.md`
  - `Monte Carlo Permutation`
  - `minimum_backtest_length`
  - `multiple_testing_report`
- Focused:
  `.venv\Scripts\python.exe -m pytest tests\shared\validation\test_realized_r.py tests\shared\validation\test_monte_carlo.py tests\shared\validation\test_multiple_testing.py tests\shared\validation\test_rule_search.py strategies\vwap_zscore_fade\tests\test_validation_run.py strategies\vwap_zscore_fade\tests\test_validation_monte_carlo_run.py tests\boundaries\test_strategy_imports.py`
  -> 36 passed.
- Full:
  `.venv\Scripts\python.exe -m pytest`
  -> 299 passed.
- `git diff --check` clean except expected LF/CRLF warnings.

## Commit Note
I did not create the requested reconciliation-only commit because the worktree
already contains earlier uncommitted validation and Monte Carlo slices, including
untracked files that this reconciliation now edits. A clean history should first
commit the already-reviewed validation/Monte Carlo slices, then this residual
reconciliation as a separate commit.

## Remaining Risks / Next Slice
- Equity-curve bootstrap is still not implemented. With residuals closed, that
  is the next Stage B slice after review.
