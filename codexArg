# Codex Argument

Change made:

- added shared indicator causality tests for `session_vwap` and `session_atr`
- added `strategies/vwap_zscore_fade/parent/params.py` as constants only
- added a local params test under the parent strategy folder
- updated pytest collection so strategy-local tests run in the normal suite
- added small regression tests for timezone-aware bar inputs, A-B-A contract
  roll marking, and direct `true_range` null OHLC validation

Why:

- causality must be pinned before parent trade-driving indicators depend on
  shared math
- parent README constants should be explicit before indicator and strategy code
  is implemented
- strategy-local tests should be included in `python -m pytest`
- the added regression tests close small review gaps without changing behavior

Files touched:

- `pyproject.toml`
- `strategies/vwap_zscore_fade/parent/params.py`
- `strategies/vwap_zscore_fade/parent/tests/test_params.py`
- `tests/shared/data/test_bars.py`
- `tests/shared/indicators/test_vwap.py`
- `tests/shared/indicators/test_volatility.py`
- `codexArg`

Verification:

- `python -m pytest tests\shared\indicators\test_vwap.py`: 12 passed
- `python -m pytest tests\shared\indicators\test_volatility.py`: 12 passed
- `python -m pytest tests\shared\data\test_bars.py`: 12 passed
- `python -m pytest strategies\vwap_zscore_fade\parent\tests\test_params.py`: 1 passed
- `python -m pytest tests\boundaries\test_strategy_imports.py`: 1 passed
- `python -m pytest`: 38 passed

Assumptions:

- simple constants in `params.py` do not need dynamic loading or a config object
- commission is intentionally not defaulted in `params.py`; run costs must be
  declared in run configuration later
- collecting `strategies/` with pytest is consistent with the repo's
  strategy-local test plan

Remaining risks:

- parent indicator integration is still unimplemented
- `prepare_bars` still uses ET calendar dates as sessions, acceptable for RTH
  v0 but not enough for future ETH or overnight work
- child parent-import boundary tests remain deferred until child strategies
  exist

Next proposed slice:

- after review, add parent-local `indicators.py` for RTH-scoped VWAP, VWAP
  deviation z-score, ATR, and volume z-score with focused tests
