# Codex Argument

Change made:

- Completed Cycle 3: added `shared/indicators/trend.py`.
- Added `adx`, `ma_slope`, `efficiency_ratio`, and `momentum`.
- Did not add blocked `EMA_Slope`, `MOM_Composite`, or `TSMOM`.
- Did not start Cycle 4 or wire these indicators into any strategy.

New file and functions:

- `shared/indicators/trend.py`
- Constants: lines 6-11
- `adx(...)`: lines 14-75
- `ma_slope(...)`: lines 78-86
- `efficiency_ratio(...)`: lines 89-100
- `momentum(...)`: lines 103-112
- Local Wilder smoothing helper: lines 115-149
- Local validation helpers: lines 152-164

ADX implementation notes:

- Wilder smoothing is implemented with an explicit loop in `_wilder_smooth`;
  pandas EWM is not used.
- Directional movement and true range are computed locally inside `adx`.
- Raw true range leaves bar 0 as NaN. To satisfy the specified smoothed TR
  seed at `window - 1`, the initial TR seed sums the non-null values in that
  first seed window; ADX smoothing itself waits for a full valid DX seed
  window, so early ADX NaNs remain intact.
- ADX is documented in-code as multi-session and does not reset at session
  boundaries.

Tests:

- Test file: `tests/shared/indicators/test_trend.py`
- Cycle 3 tests added: 14
- Test range: `tests/shared/indicators/test_trend.py:12-146`
- Helper fixture: `tests/shared/indicators/test_trend.py:149-157`

Verification:

- `.venv\Scripts\python.exe -m pytest tests\shared\indicators\test_trend.py`
  -> 14 passed
- `.venv\Scripts\python.exe -m pytest` -> 137 passed

Spec notes:

- No shared-indicator imports were added to `trend.py`.
- No strategy, discovery, runner, or research wiring changed.
- No deviations from the unlocked Cycle 3 scope.

Carried forward:

- `_last_same_session_pos` in `strategy.py` is O(N^2). Claude said not to fix
  it during the indicator build; carry this forward until a dedicated cycle.
- `robust_zscore` is missing an explicit session-reset test. Claude said to add
  it in a cleanup pass after all cycles are done.

Stopped before:

- Cycle 4.
- Any strategy or research-indicator wiring.
- Discovery rerun or slicing.
