# Codex Argument

Change made:

- Fixed the duplicated `_gap_free_rolling_window` helper called out by Claude.
- Added `shared/indicators/zscore.py` with `gap_free_rolling_window(...)`.
- Updated parent trade-driving indicators and research indicators to import the
  shared helper.
- Removed the duplicated private helper from both strategy-local files.
- Added focused shared-helper tests.

Why:

- Claude identified this duplication as an existing bug/cleanup item that must
  be fixed before any new indicator build cycles.
- This is a pure mechanical extraction of existing behavior, not a new
  indicator family or strategy behavior change.

Files touched:

- `shared/indicators/zscore.py`
- `strategies/vwap_zscore_fade/parent/indicators.py`
- `strategies/vwap_zscore_fade/parent/research_indicators.py`
- `tests/shared/indicators/test_zscore.py`
- `codexArg`

Verification:

- `.venv\Scripts\python.exe -m pytest tests\shared\indicators\test_zscore.py`
  -> 5 passed
- `.venv\Scripts\python.exe -m pytest
  strategies\vwap_zscore_fade\parent\tests\test_indicators.py
  strategies\vwap_zscore_fade\parent\tests\test_research_indicators.py`
  -> 12 passed
- `.venv\Scripts\python.exe -m pytest tests\boundaries\test_strategy_imports.py`
  -> 2 passed
- `.venv\Scripts\python.exe -m pytest` -> 104 passed

Assumptions:

- `gap_free_rolling_window(...)` is shared pure indicator support, not shared
  trading context.
- The extraction is behavior-preserving; no smoke or discovery rerun is needed
  because no strategy calculations changed.
- There is no trailing stop in the current strategy, so the old trailing-stop
  timestamping concern is closed as not applicable.

Carried forward:

- `_last_same_session_pos` in `strategy.py` is O(N^2). Claude said not to fix
  it during the indicator build; carry this forward until a dedicated cycle.

Stopped before:

- Phase 1 doc challenge.
- New shared indicator build cycles.
- Research-indicator expansion.
- Slicing.
