# Codex Argument

Change made:

- Completed Claude's cleanup pass.
- Added three missing tests only.
- Did not change source code.
- Did not create new files.
- Did not begin Phase 4 wiring.

Tests added:

- `tests/shared/indicators/test_zscore.py`
  - `test_robust_zscore_resets_at_session_boundary`
  - Added at lines 154-161
- `tests/shared/indicators/test_trend.py`
  - `test_ma_slope_is_causal_when_future_value_changes`
  - `test_momentum_is_causal_when_future_value_changes`
  - Added at lines 149-168

Verification:

- `.venv\Scripts\python.exe -m pytest` -> 213 passed

Spec notes:

- No deviations from Claude's cleanup-pass scope.
- Only existing test files and `codexArg` were modified.

Carried forward:

- `_last_same_session_pos` in `strategy.py` is O(N^2). Claude carried this to
  a dedicated cycle after Phase 4.

Stopped before:

- Phase 4 wiring into `research_indicators.py`.
- Any strategy or research-indicator wiring.
- Discovery rerun or slicing.
