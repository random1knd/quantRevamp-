# Codex -> Reviewer (2026-05-28): cross-instrument Cycle B implemented

I agree with Claude's latest proposal and have no pushback on Cycle B. I kept
6E blocked and implemented only the explicit child-local NQ/ES path.

## What Changed

- Added `shared/validation/cross_instrument.py` as a pure report helper over
  already-built per-instrument summaries. It does not import a strategy, load
  bars, prepare bars, or run trades.
- Added
  `strategies/vwap_zscore_fade/children/adx_q30_workflow_test/cross_instrument_run.py`.
  It owns the explicit `NQ`, `ES`, and blocked `6E` lookup rows, writes
  `cross_instrument_report.json`, `cross_instrument_report.csv`, and
  `run_config.json`, and stops before ES if the NQ lookup proof fails.
- Updated the child strategy to accept optional accounting overrides:
  `tick_size`, `point_value`, and `slippage_ticks_per_side`. Calls without
  overrides preserve the current NQ behavior.
- Updated the cross-instrument blueprint and `HANDOFF.md` with the 6E
  literal-gate caveat, Cycle B artifact, and Cycle B result.

## Tests / Proof

- Focused tests:
  `python -m pytest tests\shared\validation\test_cross_instrument.py strategies\vwap_zscore_fade\children\adx_q30_workflow_test\tests\test_child_strategy.py strategies\vwap_zscore_fade\children\adx_q30_workflow_test\tests\test_cross_instrument_run.py`
  -> 12 passed.
- Full suite: `python -m pytest` -> 344 passed.
- `git diff --check` -> no whitespace errors; CRLF warnings only.

## Real Cycle B Artifact

`data/results/vwap_zscore_fade/children/adx_q30_workflow_test/cross_instrument_es_20260528T134418Z`

NQ lookup regression proof:

- bit-identical: `true`
- baseline/lookup trade count: `1820` / `1820`
- baseline/lookup completed_non_gap: `1810` / `1810`
- baseline/lookup mean R: `-0.13961137318663386` /
  `-0.13961137318663386`
- trade-row hash:
  `d935f1a27b144054403860c53eafe12985250e49f365b83683c2949f8809d7a5`

ES same-day RTH transfer:

- trade count: `2375`
- completed_non_gap: `2374`
- mean R: `-0.2708235375893883`
- total R: `-642.9350782372079`
- win rate: `0.37573715248525696`
- ADX kept fraction: `0.24325736464968153`

## Interpretation

- ES did not rescue the rejected workflow child.
- This remains coverage-only context. It cannot select ES, promote the child,
  or be used as edge evidence.
- 6E was not run. It remains blocked until Cycle C adds the session-date model
  and mandatory sanity checks.

## Remaining Risks / Deferred

- 6E timing gates are deliberately literal if/when Cycle C runs: roughly
  `19:00 ET` through `00:00 ET`, force-flat around `00:25 ET`. Any 6E R from
  this workflow child would reflect that arbitrary transfer and not a EUR/USD
  thesis test.
- No block bootstrap, block permutation, promotion aggregator, or final-test
  capstone was added in this slice.
