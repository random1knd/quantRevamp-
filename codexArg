# Codex Argument

Change made:

- Acted on Claude's independent-audit instructions.
- Documented early-close handling as data-shaped policy in the parent README.
- Replaced nearest-tick stop/target rounding with conservative order-side
  rounding:
  - long stops round up
  - short stops round down
  - long targets round up
  - short targets round down
- Documented the conservative rounding policy in the parent README.
- Added the `generate_trades()` chronological/session-contiguous precondition.
- Added discovery-run protection against final-test overlap.
- Marked stale discovery artifacts superseded:
  `discovery_20260520T111227Z`, `discovery_20260521T154002Z`,
  `discovery_20260522T131103Z`, and intermediate
  `discovery_20260522T142717Z`.
- Acknowledged the weak parent discovery baseline as context only; no edge is
  being claimed before slicing.

Implementation note:

- I tightened Claude's sample final-test guard. The sample checked only
  `>= splits["test_end"]`, which catches full-dataset mistakes but misses a
  partial overlap with earlier final-test sessions. The implemented guard is
  `max(SessionDate_ET) > splits["validation_end"]`, which directly enforces
  "no final-test data" while staying local to `discovery_run.py`.

Touched files:

- `strategies/vwap_zscore_fade/parent/README.md`
- `strategies/vwap_zscore_fade/parent/strategy.py`
- `strategies/vwap_zscore_fade/parent/tests/test_strategy.py`
- `strategies/vwap_zscore_fade/parent/discovery_run.py`
- `strategies/vwap_zscore_fade/parent/tests/test_discovery_run.py`
- artifact `SUPERSEDED.txt` files under
  `data/results/vwap_zscore_fade/parent/`

Assumptions:

- Conservative target rounding means the backtest does not claim a target touch
  until the raw signal-bar VWAP has reached a valid exchange tick.
- Conservative stop rounding keeps stops no farther from entry than the raw
  modeled stop.
- The discovery runner may use validation-era dates in split metadata, but the
  discovery slice must not include any date after `validation_end`.

Verification:

- Focused tests:
  `.venv\Scripts\python.exe -m pytest strategies\vwap_zscore_fade\parent\tests\test_strategy.py strategies\vwap_zscore_fade\parent\tests\test_discovery_run.py`
  -> 36 passed
- Full suite:
  `.venv\Scripts\python.exe -m pytest` -> 246 passed
- Discovery rerun:
  `.venv\Scripts\python.exe -m strategies.vwap_zscore_fade.parent.discovery_run`
  -> `data/results/vwap_zscore_fade/parent/discovery_20260522T142846Z`

Discovery impact versus `discovery_20260522T131103Z`:

- Trade count: 3570 -> 3585, delta +15.
- Exit counts: stop 1837, time_stop 1050, target 558, session_end 135,
  gap_stop 5.
- Headline sample remains `completed_non_gap_trades`.
- Headline mean R: -0.17771123537415373.
- All-completed mean R: -0.17535146598686344.
- Tick alignment check on `EntryPrice`, `ExitPrice`, `InitialStopPrice`, and
  `InitialRisk`: zero off-tick values.

Remaining risks:

- The new discovery artifact has `code_version: "0336429-dirty"` because this
  slice has not been committed. A clean comparison artifact should be generated
  after review acceptance and commit.
- The broad parent baseline still has no demonstrated edge; slicing should
  treat it as a regime-discovery input, not an approved tradable strategy.
