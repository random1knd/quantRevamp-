# Codex -> Reviewer (2026-05-29): block-permutation coverage and durable deferrals

## What Changed

- Added missing block-permutation validation coverage in
  `tests/shared/validation/test_block_permutation.py`:
  - null `session_column` value rejects with `ValueError`
  - single-session report warns
    `single_session_block_permutation_degenerate`
  - 2..9 sessions warn `low_session_count_null_is_coarse`
  - 10+ sessions report no session-count warning
  - floor remains reported only, not enforced
- Added a durable "Deferred Until A Real Positive Candidate" section to
  `docs/overfitting_tests/README.md`.
  It records Bucket A engines as built/unwired and lists each Bucket B item with
  what it is, why deferred, what unblocks it, and the relevant spec link.

## Scope Boundary

No engine logic changed. No strategy logic, runner execution, artifact-schema
change, slicer wiring, generated artifact, promotion logic, or final-test data
was touched.

## Files Touched In This Pass

- `tests/shared/validation/test_block_permutation.py`
- `docs/overfitting_tests/README.md`
- `codexArg`

`claudeArg` was read only.

## Assumptions

- The `_block` empty-block guard is defensive for internal construction; frame
  inputs reach the existing empty-frame validation instead, so I did not contort
  the engine or tests to hit that private branch directly.
- The durable README section should carry the long-lived deferred list, while
  `HANDOFF.md` remains the session resume summary.

## Verification

- Focused validation tests:
  `.venv\Scripts\python.exe -m pytest tests\shared\validation\test_block_permutation.py tests\shared\validation\test_multiple_testing.py tests\shared\validation\test_block_bootstrap.py`
  -> 20 passed.
- Full suite:
  `.venv\Scripts\python.exe -m pytest`
  -> 364 passed.
- `git diff --check` -> no whitespace errors; CRLF warnings only.

## Remaining Risks

- Block-permutation real-data use still requires a reviewed session-key schema
  change for `context_trades.csv`; the durable docs now state this explicitly.
- The promotion aggregator and `test_role` guard remain deferred until a real
  positive candidate exists.
