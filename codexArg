# Codex Argument

Change made:

- Completed Cycle 2: expanded `shared/indicators/volatility.py`.
- Added raw rolling realized volatility on caller-provided returns.
- Added volatility and ATR percentile wrappers that delegate to
  `rolling_percentile`.
- Did not modify `true_range` or `session_atr`.
- Did not add blocked `VolRegime` or `JumpRatio`.
- Did not start Cycle 3 or touch strategy/research wiring.

Functions added:

- `realized_volatility(...)`: `shared/indicators/volatility.py:85`
- `vol_percentile(...)`: `shared/indicators/volatility.py:101`
- `atr_percentile(...)`: `shared/indicators/volatility.py:112`

Tests:

- Test file: `tests/shared/indicators/test_volatility.py`
- Existing tests retained: 12
- Cycle 2 tests added: 8
- Total volatility tests now: 20
- New test range: `tests/shared/indicators/test_volatility.py:288`

Verification:

- `.venv\Scripts\python.exe -m pytest tests\shared\indicators\test_volatility.py`
  -> 20 passed
- `.venv\Scripts\python.exe -m pytest` -> 123 passed

Spec notes:

- `realized_volatility` returns raw rolling sample standard deviation with
  pandas default `ddof=1`; no annualization is applied.
- Claude's hand-calculation note for the final 3-return window had a small
  arithmetic error. For `[0.03, -0.01, 0.02]`, pandas sample std is about
  `0.02082`, not `0.02187`; the test uses the value implied by the specified
  pandas `rolling().std()` behavior.
- Percentile wrappers only rename the delegated `rolling_percentile` output;
  bar N remains excluded from its own reference set.

Carried forward:

- `_last_same_session_pos` in `strategy.py` is O(N^2). Claude said not to fix
  it during the indicator build; carry this forward until a dedicated cycle.
- `robust_zscore` is missing an explicit session-reset test. Claude said to add
  it in a cleanup pass after all cycles are done.

Stopped before:

- Cycle 3 trend indicator module.
- Any strategy or research-indicator wiring.
- Discovery rerun or slicing.
