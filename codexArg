# Codex Argument

Change made:

- Completed Cycle 6 prerequisite: added `robust_zscore_cross_session` to
  `shared/indicators/zscore.py`.
- Completed Cycle 6: added `shared/indicators/volume.py`.
- Added `volume_ratio`, `volume_robust_zscore`, and `absorption_ratio`.
- Did not add blocked `VolRegime`, `JumpRatio`, or any other volume feature.
- Did not start Cycle 7 or wire these indicators into any strategy.

Z-score prerequisite:

- `robust_zscore_cross_session(...)`: `shared/indicators/zscore.py:93-107`
- It uses the same median/MAD formula as `robust_zscore`, but without session
  grouping, and returns name `"ZScore"`.
- New z-score tests:
  - `test_robust_zscore_cross_session_returns_value_after_window_is_met`
  - `test_robust_zscore_cross_session_returns_nan_when_mad_is_zero`

New volume file and functions:

- `shared/indicators/volume.py`
- Constants: lines 8-10
- `volume_ratio(...)`: lines 13-22
- `volume_robust_zscore(...)`: lines 25-33
- `absorption_ratio(...)`: lines 36-44
- Local validation helper: lines 47-49

Tests:

- Updated z-score test file: `tests/shared/indicators/test_zscore.py`
- New z-score test lines: `tests/shared/indicators/test_zscore.py:154-168`
- New volume test file: `tests/shared/indicators/test_volume.py`
- Cycle 6 volume tests added: 11
- Volume test range: `tests/shared/indicators/test_volume.py:12-113`

Verification:

- `.venv\Scripts\python.exe -m pytest tests\shared\indicators\test_zscore.py tests\shared\indicators\test_volume.py`
  -> 29 passed
- `.venv\Scripts\python.exe -m pytest` -> 174 passed

Spec notes:

- `volume.py` imports pandas plus the explicitly required
  `robust_zscore_cross_session` from `shared.indicators.zscore`.
- `volume_ratio` masks zero rolling means to NaN and validates positive
  windows.
- `volume_robust_zscore` delegates to `robust_zscore_cross_session`, renames
  to `"VolumeRobustZ"`, and has the required cross-session comment.
- `absorption_ratio` masks zero price change to NaN, preserves zero delta as
  `0.0`, and includes the required context-only time-bar approximation
  comment.
- No deviations from the unlocked Cycle 6 scope.

Carried forward:

- `_last_same_session_pos` in `strategy.py` is O(N^2). Claude said not to fix
  it during the indicator build; carry this forward until a dedicated cycle.
- `robust_zscore` is missing an explicit session-reset test. Claude said to add
  it in a cleanup pass after all cycles are done.
- `ma_slope` and `momentum` causality tests are also carried to cleanup.

Stopped before:

- Cycle 7.
- Any strategy or research-indicator wiring.
- Discovery rerun or slicing.
