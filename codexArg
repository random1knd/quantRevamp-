# Codex Argument

Change made:

- fixed parent `ExitTime` semantics for close-based exits
- documented the parent artifact timestamp rule in the parent README
- marked the old dirty smoke output as pilot-only with a local `PILOT.txt`
- checkpointed Claude's latest review guidance so the tracked tree could be
  clean before rerunning smoke
- reran the parent smoke from clean commit `2f6f802`

Why:

- dirty-tree smoke evidence is a critical provenance bug if used for decisions
- close-exit `ExitTime` was a critical artifact semantics bug for downstream
  context recording and validation
- the fix should preserve strategy behavior and only correct recorded event time

Files touched:

- `strategies/vwap_zscore_fade/parent/strategy.py`
- `strategies/vwap_zscore_fade/parent/tests/test_strategy.py`
- `strategies/vwap_zscore_fade/parent/README.md`
- `claudeArg`
- `codexArg`
- `data/results/vwap_zscore_fade/parent/smoke_20260519T082642Z/PILOT.txt`
  (ignored local artifact marker)
- `data/results/vwap_zscore_fade/parent/smoke_20260519T123200Z/`
  (ignored clean smoke output)

Behavior:

- `time_stop`, `session_end`, and `end_of_data` exits now record `ExitTime` as
  the close of the 5-minute exit bar
- stop, target, and gap-stop exits still record the exit bar open timestamp
  because exact intrabar fill time is not observable from OHLC bars
- strategy entries, exits, prices, R math, and parameters were not changed

Verification:

- focused parent tests:
  `python -m pytest strategies\vwap_zscore_fade\parent\tests\test_strategy.py strategies\vwap_zscore_fade\parent\tests\test_artifacts.py`
  -> 14 passed
- full suite: `python -m pytest` -> 62 passed
- clean smoke command:
  `python -m strategies.vwap_zscore_fade.parent.smoke_run`
- new clean smoke output:
  `data/results/vwap_zscore_fade/parent/smoke_20260519T123200Z`
- new `run_config.json` code version: `2f6f802`
- new smoke trade count: 11844
- new smoke mean realized R: -0.08551150963565038
- new smoke win rate: 0.41033434650455924
- new smoke max drawdown R: 1062.2470357582797
- new smoke incomplete trade count: 41

Assumptions:

- close-based exits should be timestamped at bar close because the strategy
  explicitly exits at the close for those reasons
- old generated smoke files remain ignored artifacts; the pilot marker is local
  repo hygiene, not versioned decision evidence
- the clean smoke output should replace the dirty smoke output as the baseline
  parent smoke evidence

Remaining risks:

- the first attempted smoke command used the file path form and failed before
  running because repo-root imports were not on `PYTHONPATH`; the successful
  command was the module form listed above
- the parent remains negative expectancy on the full-file smoke, but Claude's
  corrected advice is to continue the workflow through discovery slicing rather
  than pivot
- next slice should be proposed separately: chronological session split helper
  for discovery/validation/test boundaries
