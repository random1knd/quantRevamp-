# Codex Argument

Change made:

- Fixed Claude's ADX scale bug in `_wilder_smooth`.
- Tightened the shared ADX unit test to require `ADX <= 100.0`.
- Tightened the Batch 4 research wiring test to require `EntryADX <= 100.0`.
- Did not rerun discovery.
- Did not begin slicing.

Exact `_wilder_smooth` lines changed:

- Seed with first valid window:
  `result.iloc[window - 1] = initial_seed.sum()`
  -> `result.iloc[window - 1] = initial_seed.mean()`
- Normal seed:
  `result.iloc[window - 1] = seed_values.sum()`
  -> `result.iloc[window - 1] = seed_values.mean()`
- Re-seed after a NaN previous value:
  `result.iloc[position] = seed_values.sum()`
  -> `result.iloc[position] = seed_values.mean()`
- Wilder update:
  `result.iloc[position] = previous * (window - 1) / window + value`
  -> `result.iloc[position] = previous * (window - 1) / window + value / window`

Test assertions updated:

- `test_adx_detects_monotonic_uptrend` now asserts
  `20.0 < result["ADX"].iloc[-1] <= 100.0`.
- `test_batch4_adx_is_nan_early_and_non_nan_eventually` now stores non-NaN
  `EntryADX` values and asserts all are `<= 100.0`.

ADX sanity check output:

```text
[100.0, 100.0, 100.0]
```

Verification:

- `.venv\Scripts\python.exe -m pytest tests\shared\indicators\test_trend.py strategies\vwap_zscore_fade\parent\tests\test_research_indicators.py`
  -> 42 passed
- `.venv\Scripts\python.exe -m pytest` -> 233 passed

Errors or deviations:

- None from Claude's ADX bugfix instructions.

Stopped before:

- Discovery rerun.
- Slicing.
- Any strategy/trade-generating changes.

Carried forward:

- `_last_same_session_pos` in `strategy.py` remains O(N^2), carried forward by
  Claude to a dedicated fix cycle after this ADX fix is confirmed.
