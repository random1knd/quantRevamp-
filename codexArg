# Codex Argument

Change made:

- Completed Cycle 7: added `shared/indicators/candle.py`.
- Completed Cycle 7: added `shared/indicators/time_context.py`.
- Added candle helpers `body_ratio` and `close_position`.
- Added time-context helpers `session_progress`, `bars_since_open`,
  `hour_of_day`, and `day_of_week`.
- Did not add blocked `CandleType`.
- Did not start Cycle 8 or wire these indicators into any strategy.

Candle file and functions:

- `shared/indicators/candle.py`
- Constants: lines 6-7
- `body_ratio(...)`: lines 10-19
- `close_position(...)`: lines 22-30

Time-context file and functions:

- `shared/indicators/time_context.py`
- Constants: lines 6-9
- `session_progress(...)`: lines 12-35
- `bars_since_open(...)`: lines 38-44
- `hour_of_day(...)`: lines 47-54
- `day_of_week(...)`: lines 57-64

Tests:

- New candle test file: `tests/shared/indicators/test_candle.py`
- Candle tests added: 9
- Candle test range: `tests/shared/indicators/test_candle.py:7-114`
- New time-context test file: `tests/shared/indicators/test_time_context.py`
- Time-context tests added: 9
- Time-context test range:
  `tests/shared/indicators/test_time_context.py:14-110`

Verification:

- `.venv\Scripts\python.exe -m pytest tests\shared\indicators\test_candle.py tests\shared\indicators\test_time_context.py`
  -> 18 passed
- `.venv\Scripts\python.exe -m pytest` -> 192 passed

Spec notes:

- Both new modules import pandas only.
- No shared indicator imports were added.
- Candle functions mask zero ranges to NaN and set the requested names.
- `session_progress` converts timezone-aware input to the requested timezone,
  uses the open-inclusive and close-exclusive session mask, and names the
  output `"SessionProgress"`.
- `bars_since_open` intentionally takes only `session`, matching Claude's
  instruction to drop the misleading unused `datetime_series` argument.

Fixture deviations from Claude's test text:

- The day-of-week test uses `2024-01-15 14:30:00 UTC` instead of
  `2024-01-15 00:00:00 UTC`. Midnight UTC converts to Sunday evening in
  `"America/New_York"`, while 14:30 UTC converts to Monday 09:30 ET and tests
  the specified timezone conversion behavior.

Carried forward:

- `_last_same_session_pos` in `strategy.py` is O(N^2). Claude said not to fix
  it during the indicator build; carry this forward until a dedicated cycle.
- `robust_zscore` is missing an explicit session-reset test. Claude said to add
  it in a cleanup pass after all cycles are done.
- `ma_slope` and `momentum` causality tests are also carried to cleanup.

Stopped before:

- Cycle 8.
- Any strategy or research-indicator wiring.
- Discovery rerun or slicing.
