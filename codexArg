# Codex Argument

Change made:

- added parent-local `indicators.py` for the VWAP z-score fade parent
- added focused parent indicator tests
- changed the strategy import boundary scan to skip strategy-local `tests/`
- updated `IMPLEMENTATION_PLAN.md` to use
  `LAST_RTH_BAR_OPEN_SESSION_MINUTE`

Why:

- Claude approved moving to parent indicators and asked for the boundary-scan
  and plan-name fixes in the same slice
- parent indicator behavior belongs in the strategy folder because it applies
  this strategy's RTH scoping and window choices
- shared indicator functions are still only used for pure VWAP and ATR math

Files touched:

- `strategies/vwap_zscore_fade/parent/indicators.py`
- `strategies/vwap_zscore_fade/parent/tests/test_indicators.py`
- `strategies/vwap_zscore_fade/parent/IMPLEMENTATION_PLAN.md`
- `tests/boundaries/test_strategy_imports.py`
- `codexArg`

Behavior added:

- computes `TypicalPrice`, `SessionVWAP`, `VWAPDeviation`, `EntryZ`, `ATR`,
  `EntryVolumeZ`, `EntryDelta`, and `EntryDeltaPct`
- scopes indicator calculations to RTH bar-open minutes 0 through 385
- leaves non-RTH rows present but with indicator columns missing
- resets rolling calculations by `SessionDate_ET`
- uses full windows only for z-scores and ATR

Verification:

- `python -m pytest strategies\vwap_zscore_fade\parent\tests\test_indicators.py`: 4 passed
- `python -m pytest tests\boundaries\test_strategy_imports.py`: 2 passed
- `python -m pytest`: 43 passed

Assumptions:

- `ATR` is the indicator column name; later trade rows can record the selected
  signal-bar value as `SignalATR`
- `EntryZ` follows the README rule of dividing VWAP deviation by rolling
  standard deviation without subtracting a rolling mean
- strategy-local tests may import context or research modules later without
  being treated as trade-generating code

Remaining risks:

- parent `strategy.py` is still unimplemented
- same-bar stop/target handling and realized-R accounting remain deferred until
  simulator or strategy trade objects exist
- `SessionDate_ET` remains an RTH-v0 calendar-date session key, not a full
  futures overnight session model

Next proposed slice:

- add parent `strategy.py` and focused tests for one long signal and one short
  signal, keeping entry/exit/stop rules local and avoiding runner or simulator
  abstractions
