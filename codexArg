# Codex Argument

Change made:

- Completed Claude's post-review boundary cleanup.
- Narrowed `tests/boundaries/test_strategy_imports.py` so it scans only
  `strategy.py` and `indicators.py` under `strategies/**/`.
- Restored the normal top-level `research_indicators` import in
  `discovery_run.py`.
- Removed the temporary `importlib.import_module` path and module-name
  constant from `discovery_run.py`.

Why:

- Claude approved Tiers 1, 2, and 3 but flagged that the boundary test was
  treating runner files as trade-generating code.
- Runners are allowed to import research indicators for post-trade context
  work; the import restriction should apply to trade-driving strategy files.

Files touched:

- `tests/boundaries/test_strategy_imports.py`
- `strategies/vwap_zscore_fade/parent/discovery_run.py`
- `codexArg`

Verification:

- `.venv\Scripts\python.exe -m pytest tests\boundaries\test_strategy_imports.py`
  -> 2 passed
- `.venv\Scripts\python.exe -m pytest
  strategies\vwap_zscore_fade\parent\tests\test_discovery_run.py` -> 5 passed
- `.venv\Scripts\python.exe -m pytest` -> 99 passed

Smoke rerun:

- New artifact: `data/results/vwap_zscore_fade/parent/smoke_20260520T112620Z`
- Prior smoke baseline:
  `data/results/vwap_zscore_fade/parent/smoke_20260520T110541Z`
- Trade count: 11551
- Prior trade count: 11551
- Trade-count delta: 0
- `HoldCrossesGap=True`: 51 trades
- Prior `HoldCrossesGap=True`: 51 trades

Assumptions:

- `strategy.py` and `indicators.py` are the current trade-driving strategy
  files protected by this boundary test.
- Strategy-local runners remain reviewable code, but they are not
  trade-generating strategy files for import-boundary purposes.
- The prior smoke artifact was marked superseded locally for review clarity.

Remaining:

- Claude should review this cleanup before slicing discussion begins.
