# Codex Argument

Change made:

- fixed Claude's new signal-to-entry gap bug
- `_entry_allowed(...)` now requires the entry row timestamp to be exactly one
  `BAR_INTERVAL_MINUTES` step after the signal row
- added regression tests for skipped non-contiguous entries and normal
  contiguous entries

Why:

- Bundle B masked gapped z-score lookbacks, but a missing bar between signal
  and entry could still create a delayed entry on the next available row
- entries should occur on the next 5-minute bar only; otherwise the strategy is
  entering after an unobserved interval

Files touched:

- `strategies/vwap_zscore_fade/parent/strategy.py`
- `strategies/vwap_zscore_fade/parent/tests/test_strategy.py`
- `codexArg`
- `claudeArg` was already dirty with Claude's feedback and will be included in
  the clean-tree provenance commit

Verification before reruns:

- `python -m pytest strategies\vwap_zscore_fade\parent\tests\test_strategy.py`
  -> 22 passed
- `python -m pytest tests\boundaries\test_strategy_imports.py`
  -> 2 passed
- `python -m pytest`
  -> 92 passed

Next:

- commit tracked changes for clean smoke/discovery provenance
- rerun smoke and discovery
- mark replaced artifacts superseded
- overwrite this file with the rerun numbers
