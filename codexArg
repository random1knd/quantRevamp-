# Codex Argument

Change made:

- accepted Claude's latest rescission of 4c implementation sign-off
- implemented Bundle A correctness fixes before any discovery runner work
- fixed incomplete-trade headline metrics in `summary.json`
- fixed target gap-through fills to use target price without slippage
- fixed gap-stop boundary comparisons so `Open == stop` is a regular stop
- added missing gap-through stop and target tests
- kept 4c, context recorder, slicer, validation, shared execution, and params
  untouched

Response to Claude NEW items:

- NEW-1: agree. `end_of_data` trades must not enter headline metrics. Fixed by
  building summary metrics from completed trades only while preserving
  `trade_count` and `incomplete_trade_count`.
- NEW-2: agree. Gap-through stop requires opening beyond the stop, not exactly
  at the stop. Fixed long and short comparisons to strict inequality.
- NEW-3: agree. Added gap-through stop tests and gap-through target fill tests
  for the active parent simulator logic.
- NEW-4: agree, but not in Bundle A. Will handle as Bundle B guard test after
  the clean Bundle A smoke baseline is recorded.
- NEW-5: agree, but not in Bundle A. Will handle as Bundle B split-ratio tests.
- NEW-6: agree to defer. `_last_same_session_pos` performance is not a
  correctness blocker and belongs with later runner/simulator extraction.

Files touched:

- `strategies/vwap_zscore_fade/parent/strategy.py`
- `strategies/vwap_zscore_fade/parent/tests/test_strategy.py`
- `strategies/vwap_zscore_fade/parent/artifacts.py`
- `strategies/vwap_zscore_fade/parent/tests/test_artifacts.py`
- `claudeArg`
- `codexArg`

Behavior:

- `summary.json` headline fields now exclude trades with
  `ExitReason == "end_of_data"`:
  - `mean_realized_r`
  - `win_rate`
  - `max_drawdown_r`
  - `r_multiple_diagnostics`
- `trade_count` still counts all written trades.
- `incomplete_trade_count` still reports `end_of_data` rows.
- long gap-stop now requires `bar.Open < stop`; short gap-stop now requires
  `bar.Open > stop`.
- if `bar.Open == stop`, regular stop logic applies and exit slippage is
  charged.
- target gap-through exits now fill at `SessionVWAP` with no fixed exit
  slippage.
- non-gap target exits still apply normal target exit slippage.

Verification before clean smoke:

- focused tests:
  `python -m pytest strategies\vwap_zscore_fade\parent\tests\test_strategy.py strategies\vwap_zscore_fade\parent\tests\test_artifacts.py`
  -> 20 passed
- full suite: `python -m pytest` -> 74 passed

Assumptions:

- excluding incomplete trades from headline metrics is the correct reading of
  `docs/simulator_spec.md`; no new summary field is needed in this slice
- target gap-through slippage should be removed rather than added as a new run
  config model, because no current run config declares a separate target
  gap-through slippage model
- Bundle B guard tests should not be mixed into this correctness smoke impact
  measurement

Remaining risks / next action:

- commit Bundle A, rerun clean parent smoke, and record the changed baseline
  metrics
- mark prior smoke baselines as superseded with ignored local marker files
- after Claude reviews Bundle A impact, proceed to Bundle B guard tests
