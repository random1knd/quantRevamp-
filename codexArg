# Codex Argument

Change made:

- Completed Claude's Phase 4 Batch 4 final research-indicator wiring batch.
- Added 5 research-context columns to
  `strategies/vwap_zscore_fade/parent/research_indicators.py`.
- Added 6 Batch 4 wiring tests to
  `strategies/vwap_zscore_fade/parent/tests/test_research_indicators.py`.
- Did not change strategy trade generation.
- Did not run discovery.

Exact imports added:

- `from shared.indicators.regime import rolling_autocorr, rolling_variance_ratio`
- `from shared.indicators.time_context import bars_since_open`
- `from shared.indicators.trend import adx, efficiency_ratio`

Exact columns added:

- `EntryAutoCorr`
- `EntryVarRatio`
- `EntryADX`
- `EntryEfficiencyRatio`
- `EntryBarsSinceOpen`

Exact wiring lines added:

- `rth["EntryAutoCorr"] = rolling_autocorr(rth["Close"], window=20, lag=1)`
- `rth["EntryVarRatio"] = rolling_variance_ratio(rth["Close"], window=20, q=2)`
- `rth["EntryADX"] = adx(rth, window=14)["ADX"]`
- `rth["EntryEfficiencyRatio"] = efficiency_ratio(rth["Close"], window=20)`
- `rth["EntryBarsSinceOpen"] = bars_since_open(session=rth["SessionDate_ET"])`

Tests added and status:

- `test_batch4_autocorr_is_nan_before_window_and_one_for_linear_trend`
  -> passed
- `test_batch4_var_ratio_is_nan_for_constant_step_close`
  -> passed
- `test_batch4_adx_is_nan_early_and_non_nan_eventually`
  -> passed
- `test_batch4_efficiency_ratio_is_nan_before_window_and_one_for_trend`
  -> passed
- `test_batch4_bars_since_open_starts_at_zero_and_resets_at_session_boundary`
  -> passed
- `test_batch4_columns_are_nan_for_non_rth_rows`
  -> passed

Verification:

- `.venv\Scripts\python.exe -m pytest` -> 233 passed

Deviations:

- None from Claude's Batch 4 instructions.

Assumptions:

- Claude's specified windows and parameters are the boundary spec for this
  wiring pass.
- Batch 4 remains research-context only.

Remaining risks:

- New Entry* columns are wired for research context only and still require
  downstream slicing/review before any trade-generating use.
- `_last_same_session_pos` in `strategy.py` remains O(N^2), carried forward by
  Claude to a dedicated cycle after Phase 4 batches.
