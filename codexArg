Codex child-slice handoff: ADX q30 workflow-test child.

Pushback:
- None. Claude's instruction is consistent with the repo rules: the child is a
  separate reviewable slice, explicitly labeled as a workflow test, and does not
  revise the honest slicer `no_candidate` verdict.

What changed:
- Added child folder:
  `strategies/vwap_zscore_fade/children/adx_q30_workflow_test/`
- Added child files:
  - `README.md`
  - `strategy.py`
  - `indicators.py`
  - `params.py`
  - `evidence/filter_candidate.json`
  - `evidence/slicer_plan.json`
  - `tests/test_child_strategy.py`

Child label:
- `workflow test / demonstration child, negative expectancy, NOT a discovered edge`
- The README states this explicitly. Future run artifacts for this child must
  carry the same label.

Behavior implemented:
- Parent strategy logic was copied into the child; the child does not import or
  wrap the parent strategy.
- `SignalADX` is now computed in the child trade path in `indicators.py`, not
  imported from research context.
- ADX is session-scoped and causal, matching the research-context semantics.
- Entry filter:
  `ADX <= 19.26665446628932`
- The threshold is frozen from the no-candidate slicer artifact's best eligible
  rule: `SignalADX__le__q30`.
- ADX NaN at the signal bar blocks entry.
- Parent gates remain copied: 60-minute post-open gate, roll-session exclusion,
  stop/target/time-stop/session-end logic, slippage, commission checks, and
  wrong-side rounded-stop guard.

Tests added:
- Frozen ADX threshold matches copied slicer evidence.
- Child ADX is causal when future bars are mutated.
- Child ADX resets at the session boundary.
- Child strategy skips a valid parent-style signal when ADX is missing.
- Child entry gate blocks ADX above threshold and allows ADX equal to threshold.
- Child indicator columns include trade-driving `ADX`.

Boundary:
- Child strategy files do not import parent modules, `research_indicators`,
  `shared.context`, or `shared.slicing`.
- Boundary import test passes.

Verification:
- Focused child + boundary:
  `.venv\\Scripts\\python.exe -m pytest strategies\\vwap_zscore_fade\\children\\adx_q30_workflow_test\\tests\\test_child_strategy.py tests\\boundaries\\test_strategy_imports.py`
  -> 9 passed.
- Full suite:
  `.venv\\Scripts\\python.exe -m pytest`
  -> 276 passed.

Remaining risks / next slice:
- The child has not been validated yet. It is expected to lose and should be
  rejected honestly.
- Next reviewable slice should build the validation runner/report for parent vs
  demonstration child on the 50% validation split, with artifacts labeled
  workflow-test coverage / not edge evidence.
