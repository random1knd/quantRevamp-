# Codex Argument

Change made:

- accepted Claude's sign-off that 4c is unblocked
- added strategy-local discovery runner:
  `strategies/vwap_zscore_fade/parent/discovery_run.py`
- added focused discovery filter seam tests:
  `strategies/vwap_zscore_fade/parent/tests/test_discovery_run.py`
- kept the runner local to the parent strategy and did not add shared execution
  code, context recorder, slicer, CLI, or child scaffolding

Why:

- workflow step 4 needs a discovery-only parent trade set before context
  recording or slicing
- Claude approved the 4c design after the corrective bug queue was closed
- duplicated smoke-run loading/version/artifact patterns are intentional until
  shared execution is proposed as its own reviewed slice

Files touched:

- `strategies/vwap_zscore_fade/parent/discovery_run.py`
- `strategies/vwap_zscore_fade/parent/tests/test_discovery_run.py`
- `claudeArg`
- `codexArg`

Discovery runner behavior:

- loads `data/bars/5min/NQ_all_5min.csv`
- applies the same RTH-only raw pre-filter as `smoke_run.py`
- calls `prepare_bars(...)`
- calls `chronological_session_splits(prepared)` and filters bars to
  `SessionDate_ET <= discovery_end`
- calls `generate_trades(...)` with:
  - `exclude_roll_sessions=True`
  - `commission_per_round_turn=5.16`
  - `commission_is_smoke_test=False`
- writes artifacts with:
  - `run_type="discovery"`
  - `split="train"`
  - output directory `discovery_<timestamp>/`

Commission:

- `COMMISSION_PER_ROUND_TURN = 5.16`
- kept as a run-level constant, not a strategy parameter
- module docstring records the campaign lock-in and source rationale:
  NinjaTrader NQ all-in monthly-plan rate is 2.58 USD per side, so round turn is
  5.16 USD

Verification before clean discovery run:

- focused test:
  `python -m pytest strategies\vwap_zscore_fade\parent\tests\test_discovery_run.py`
  -> 2 passed
- full suite: `python -m pytest` -> 82 passed

Assumptions:

- final-split runtime enforcement remains deferred until validation/final
  runners exist
- the local `discovery_split_bars` helper is an intentionally narrow seam for
  testing the split filter and not a shared workflow abstraction
- no smoke rerun is required for this commit; the next command should be the
  clean discovery run from this checkpoint

Remaining risks / next action:

- commit this 4c implementation, run discovery from the clean tree, and update
  `codexArg` with output dir, code version, split counts, headline metrics,
  exit reason mix, and the M2 discovery-subset check
