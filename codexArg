# Codex Argument

Change made:

- Completed Cycle 8: expanded `shared/indicators/vwap.py`.
- Added `vwap_distance` and `vwap_distance_atr_normalized`.
- Added constants `VWAP_DISTANCE_NAME` and `VWAP_DISTANCE_ATR_NAME`.
- Did not touch `typical_price` or `session_vwap` behavior.
- Did not add blocked `VWAPDist_SD`.
- Did not start Cycle 9 or wire these indicators into any strategy.

VWAP file additions:

- `shared/indicators/vwap.py`
- New constants: lines 7-8
- `vwap_distance(...)`: lines 19-25
- `vwap_distance_atr_normalized(...)`: lines 28-34

Tests:

- Updated test file: `tests/shared/indicators/test_vwap.py`
- Cycle 8 tests added: 8
- New test range: `tests/shared/indicators/test_vwap.py:243-309`
- New test names:
  - `test_vwap_distance_returns_positive_when_close_above_vwap`
  - `test_vwap_distance_returns_negative_when_close_below_vwap`
  - `test_vwap_distance_returns_nan_where_vwap_is_nan`
  - `test_vwap_distance_atr_normalized_returns_known_value`
  - `test_vwap_distance_atr_normalized_returns_nan_where_atr_is_zero`
  - `test_vwap_distance_atr_normalized_returns_nan_where_atr_is_nan`
  - `test_vwap_distance_is_causal_when_future_values_change`
  - `test_vwap_distance_atr_normalized_is_causal_when_future_values_change`

Verification:

- `.venv\Scripts\python.exe -m pytest tests\shared\indicators\test_vwap.py`
  -> 20 passed
- `.venv\Scripts\python.exe -m pytest` -> 200 passed

Spec notes:

- `vwap.py` remains pandas-only.
- `vwap_distance` returns `close - vwap` and allows NaNs to propagate
  naturally.
- `vwap_distance_atr_normalized` masks zero ATR to NaN and naturally propagates
  NaN ATR.
- No deviations from the unlocked Cycle 8 scope.

Carried forward:

- `_last_same_session_pos` in `strategy.py` is O(N^2). Claude said not to fix
  it during the indicator build; carry this forward until a dedicated cycle.
- `robust_zscore` is missing an explicit session-reset test. Claude said to add
  it in a cleanup pass after all cycles are done.
- `ma_slope` and `momentum` causality tests are also carried to cleanup.

Stopped before:

- Cycle 9.
- Any strategy or research-indicator wiring.
- Discovery rerun or slicing.
