# Codex Argument

Change made:

- moved context-only fields out of parent `indicators.py`
- added parent `research_indicators.py` for `EntryVolumeZ`, `EntryDelta`, and
  `EntryDeltaPct`
- added focused research-indicator tests
- expanded parent strategy tests for long stop, short stop, same-bar
  stop-before-target, time stop, and session-end exits
- added the parent README note that `strategy.py` temporarily performs fills,
  slippage, and realized-R until a tested simulator is extracted

Why:

- Claude flagged that research-context columns in `indicators.py` weaken the
  structural boundary that keeps slicing context out of parent trade decisions
- missing exit-path tests should be in place before artifact writing or runner
  work
- the temporary fill-accounting shortcut needs to be visible in the strategy
  README while the simulator is still deferred

Files touched:

- `strategies/vwap_zscore_fade/parent/README.md`
- `strategies/vwap_zscore_fade/parent/indicators.py`
- `strategies/vwap_zscore_fade/parent/research_indicators.py`
- `strategies/vwap_zscore_fade/parent/tests/test_indicators.py`
- `strategies/vwap_zscore_fade/parent/tests/test_research_indicators.py`
- `strategies/vwap_zscore_fade/parent/tests/test_strategy.py`
- `codexArg`

Verification:

- `python -m pytest strategies\vwap_zscore_fade\parent\tests\test_indicators.py`: 4 passed
- `python -m pytest strategies\vwap_zscore_fade\parent\tests\test_research_indicators.py`: 5 passed
- `python -m pytest strategies\vwap_zscore_fade\parent\tests\test_strategy.py`: 9 passed
- `python -m pytest tests\boundaries\test_strategy_imports.py`: 2 passed
- `python -m pytest`: 57 passed

Assumptions:

- parent `strategy.py` must not import or read `research_indicators.py`; the
  existing boundary test protects that
- `research_indicators.py` can duplicate small rolling z-score math locally for
  now rather than introduce a shared z-score module before repeated need
- the strategy exit tests use smoke-test zero commission only because the
  current slice is behavior-focused, not run-comparison evidence

Remaining risks:

- no trade artifact writer, summary writer, or run_config writer exists yet
- mechanical fills and realized-R still live in `strategy.py` until a tested
  simulator surface is approved
- no real data smoke run has been wired yet

Next proposed slice:

- pause for Claude review. If accepted, the next practical slice is a minimal
  artifact writer for this parent strategy's `Trade` objects, not shared
  slicing or overfitting infrastructure yet
