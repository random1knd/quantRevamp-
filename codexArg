# Codex Argument

Change made:

- fixed parent `ExitTime` semantics for close-based exits
- documented the parent artifact timestamp rule in the parent README
- marked the existing dirty smoke output as pilot-only with a local `PILOT.txt`
- kept the fix inside the parent strategy slice; no runner, registry, split
  helper, or broad framework was added

Why:

- Claude was right that dirty-tree smoke evidence is a critical provenance
  problem if it is treated as decision evidence
- Claude's `ExitTime` objection is also critical for artifact semantics because
  downstream context recording and validation can misinterpret close exits
- PnL math was not changed, but the artifact timestamp now matches the declared
  fill event for `time_stop`, `session_end`, and `end_of_data`

Files touched:

- `strategies/vwap_zscore_fade/parent/strategy.py`
- `strategies/vwap_zscore_fade/parent/tests/test_strategy.py`
- `strategies/vwap_zscore_fade/parent/README.md`
- `data/results/vwap_zscore_fade/parent/smoke_20260519T082642Z/PILOT.txt`
  (ignored local artifact marker)
- `claudeArg` already contained Claude's latest review and is being kept as the
  current accepted reviewer input
- `codexArg`

Behavior:

- `time_stop`, `session_end`, and `end_of_data` exits now record `ExitTime` as
  `DateTime_ET + BAR_INTERVAL_MINUTES`
- stop, target, and gap-stop exits still record the exit bar open timestamp
  because exact intrabar fill time is not observable from OHLC bars
- old smoke output remains unchanged except for the pilot marker

Verification so far:

- `python -m pytest strategies\vwap_zscore_fade\parent\tests\test_strategy.py strategies\vwap_zscore_fade\parent\tests\test_artifacts.py`:
  14 passed
- `python -m pytest`: 62 passed

Assumptions:

- the correct timestamp for close-based exits is the close of the 5-minute exit
  bar
- the dirty `claudeArg` review should be checkpointed rather than discarded,
  because it is the current reviewer guidance the user asked me to act on
- generated `data/results` files remain ignored, so the pilot marker is a local
  annotation rather than versioned source evidence

Remaining risks:

- a clean smoke rerun still needs to be executed from a clean tracked tree after
  this checkpoint
- the new clean smoke output should be inspected only for deterministic
  equivalence on trade count and headline metrics; it should not trigger tuning
  or slicing in this same slice
