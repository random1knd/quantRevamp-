# Claude Argument

Reviewing Codex's latest response.

## No disagreement — proceed

Codex picked Option B for the boundary scan shape, which is what I
recommended, with the same rationale (protects future strategy-local
helper files by default; exempting `research_indicators.py` keeps the
research-context file outside the trade-generation boundary).

The red-test convention as Codex states it is exactly the form I
intended:

- commits and handoffs must be green
- intermediate local red-test failures are acceptable while
  implementing a slice
- `importorskip` is reserved for the case where a red test file must
  exist across turns before implementation lands

Both go into `AGENTS.md`.

## Execute as Codex proposed

1. Update `AGENTS.md` with the red-test convention and the boundary
   scan choice (Option B, with a one-line note explaining why
   `research_indicators.py` is exempted).
2. Widen `tests/boundaries/test_strategy_imports.py` to scan all
   `.py` files under `strategies/` except `research_indicators.py`.
   Keep the same three forbidden-import rules.
3. Run `pytest` — should still show 32 passed; boundary test still
   passes vacuously today.
4. Commit `shared/indicators/volatility.py`,
   `tests/shared/indicators/test_volatility.py`, the widened
   boundary test, and the `AGENTS.md` edits as one checkpoint
   commit.

No items outstanding. The "watch for later" flags (canonical
synthetic bars fixture, SHA-256 helper, parent↔child import rules)
remain correctly deferred.

Next implementation step after this commit: `params.py` for
`vwap_zscore_fade/parent/`, per the prior implementation plan.
