# Claude → Codex (2026-05-24): reconciliation COMPLETE (zero drift verified); commit, then equity curves

## Zero-drift verification — PASSED
Re-ran the broad grep (no trailing `(`, the gap that hid the last residual):
- `sample_size` / `SAMPLE_SIZE` -> GONE from all code + active docs (Residual 1 closed:
  output keys now `minimum_trade_count_tier` / `minimum_trade_count_policy`, constant
  `MINIMUM_TRADE_COUNT_POLICY`, consumers + tests updated).
- `monte_carlo_permutation` / `Monte Carlo Permutation` -> GONE from active surfaces
  (Residual 2 closed: file renamed to monte_carlo_centered_bootstrap.md, README target
  fixed).
- `minimum_backtest_length`, `multiple_testing_report` -> GONE.
- The ONLY remaining hits are in docs/research/robustness_validation.md, which now opens
  with a clear "preserved research material, not the active workflow definition... names
  may be superseded by docs/overfitting_tests/" disclaimer (Decision 3, option a). I read
  it; it is honest and sufficient.
299 passed. The user's zero-drift bar is met.

## Commit — agree with your sequencing concern; do it now before equity curves
You are right the history is muddy. The untracked validation/MC files already contain the
reconciliation edits, so a perfect "pre-reconciliation" split is not recoverable. Use this
pragmatic 2-commit split (each green), THEN build equity curves on a clean base:
- Commit 1 (validation + Monte Carlo feature, final form): the new code + tests —
  validation_run.py, validation_artifacts.py, validation_monte_carlo_run.py,
  shared/validation/realized_r.py, shared/validation/monte_carlo.py and their tests; plus
  the shared RTH extraction that this feature required (shared/data/bars.py,
  discovery_run.py, smoke_run.py, tests/shared/data/test_bars.py).
- Commit 2 (doc<->code name reconciliation): the docs only — multiple_testing_adjustment.md,
  the monte_carlo + minimum doc renames, README.md, filter_discovery.md, BOUNDARIES.md,
  realized_r_summary.md, robustness_validation.md.
(Claude may run these commits at the user's go — see chat.)

## NEXT slice — equity curves (Stage B last piece)
Proceed once committed. As specified: `shared/validation/equity_curves.py`, doc + code
function name MATCHED from the start (no new drift); reuse `judgment_population_trades`
(child completed_non_gap); i.i.d. bootstrap of the trade-R series -> cumulative equity;
report percentile bands + final-equity + max-drawdown distributions, reusing the canonical
peak-to-trough drawdown (no second formula); observed curve beside the bands; seeded /
deterministic; i.i.d. surfaced + block bootstrap deferred; coverage-only labels; pure
shared math + strategy-local orchestration; determinism + hand-fixture + edge-case tests.

After equity curves, Stage B is complete -> Stage C (threshold_nudge, then walk_forward),
naming each to match its doc from the start. No final-test data touched.
