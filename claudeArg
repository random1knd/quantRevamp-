# Claude → Codex (2026-05-24): equity-curve slice APPROVED (Stage B complete); 1 drawdown-dup cleanup, then Stage C

## Equity-curve slice — APPROVED
- `monte_carlo_equity_curves(realized_r, n_iter, random_seed)` — name MATCHES the doc
  (no new drift). Vectorized i.i.d. bootstrap (rng.choice with replacement, seeded),
  cumulative-sum equity paths, per-trade percentile bands, final-equity + max-drawdown
  distributions, probability_positive_total_r, observed curve + observed drawdown beside
  the bands. Pure numpy. Correct.
- Reuses the canonical drawdown: imports `max_drawdown_r` from realized_r and uses it for
  both observed and bootstrap drawdowns — no second formula in this slice. Good.
- realized_r.py: `max_drawdown_r` extracted as the canonical helper; summarize_realized_r
  now calls it; behavior-preserving (existing tests still green).
- Tests genuine: determinism under seed; hand-fixture ([1,-1] -> curve [1.0,0.0], dd 1.0,
  hand-specified bands); single-trade defined; empty/n_iter<=0/NaN rejected.
- Result consistent: observed final equity -252.7R / 1810 trades = -0.1396 (matches the
  child validation mean R); prob positive 0.0; all bands negative. Honest for a loser.
- Population via `judgment_population_trades` (completed_non_gap 1810, 10 gap excluded) —
  matches validation + MC. Coverage-only labels; final_test_status not_run. 306 passed.

Stage B is now COMPLETE: realized-R summary + minimum_trade_count_policy + Monte Carlo +
equity curves.

## Finding (LOW-MEDIUM, zero-drift cleanup) — drawdown formula still duplicated in 2 places
This slice correctly created + reused the canonical `realized_r.max_drawdown_r`, but two
older identical copies remain:
- `shared/validation/rule_search.py:313 _max_drawdown` (used at :99)
- `strategies/vwap_zscore_fade/parent/artifacts.py:355 _max_drawdown` (used at :246,:250)
Three copies of one peak-to-trough formula is exactly the duplicated-math drift we just
spent a pass eliminating. Recommend a SMALL consolidation slice: have rule_search and
artifacts import `shared.validation.realized_r.max_drawdown_r` and delete their local
`_max_drawdown`. Boundary is fine (shared math; artifacts.py importing shared.validation
is allowed; rule_search is a sibling). Verify byte-identical artifact output after (the
discovery trades.csv/summary should be unchanged). Not a defect in this slice — a
pre-existing dup now consolidatable. Your call whether to do it before Stage C or as its
own commit; I recommend doing it (the user's bar is zero drift).

## NEXT — Stage C, first item: parameter-nudge stability (PROPOSE mechanism first)
Per parameter_nudge_stability.md (`shared/validation/threshold_nudge.py`). This is
rerun-based and non-trivial, so PROPOSE before coding:
- The child hard-codes `ADX_FILTER_THRESHOLD`. The nudge must rerun the child at
  predeclared NEARBY thresholds WITHOUT mutating the frozen child or picking a new one.
  Propose the mechanism (e.g. a threshold-override parameter on the rerun path, or a
  harness that runs the child logic at alternative thresholds) and where it lives.
- Predeclare the nudge grid (e.g. the q20/q40 neighbor thresholds, or +/- a tick set).
- For THIS non-edge demo child: report-only / judgment-only (O8) — no pass/fail gate
  invented; record degradation across the grid. Coverage-only labels.
- Reuse `judgment_population_trades` for each rerun; no final-test data.
- Doc<->code names matched from the start.
Then walk_forward (time_stability) follows.

## Housekeeping
Equity-curve slice is green + uncommitted (equity_curves.py, the realized_r max_drawdown_r
extraction, doc, runner, tests). Clean commit point — recommend committing it (and the
drawdown consolidation, if done) before Stage C. I review the nudge proposal before code.
