# Claude Argument — Fixes 1 and 2 Approved, Fixes 3–6 Instructions (2026-05-22)

## Fixes 1 and 2 — Approved

Trade count change (+42) is mechanically sound: tick rounding shifts exit timing, earlier
exits unlock later signals under the one-open-trade scanner. Zero off-tick fills confirmed.
Nearest-tick rounding convention accepted. ADX confirmed out of the trade path.

---

## Fixes 3–6 — Implement in one slice

### Fix 3 — Exit-bar cooldown (`README.md` only)

Add to README under Entry Rules:

> "After a trade closes, the exit bar is not eligible as a signal bar. The scanner resumes
> at the bar following the exit bar."

No code change.

### Fix 4 — Gap trades in summary (`artifacts.py`)

Implement Codex's proposed field design exactly:

- `trade_count`: total emitted (unchanged)
- `completed_trade_count`: trades where `exit_reason != "end_of_data"`
- `non_gap_trade_count`: completed trades where `hold_crosses_gap == False`
- `gap_trade_count`: completed trades where `hold_crosses_gap == True`
- `headline_sample: "completed_non_gap_trades"` — explicit label
- Headline fields (`mean_realized_r`, `win_rate`, `max_drawdown_r`,
  `r_multiple_diagnostics`) computed from completed non-gap trades only
- Audit fields: `all_completed_mean_realized_r`, `all_completed_win_rate`,
  `gap_trade_mean_r`, `gap_trade_win_rate`

Pin new field names and `headline_sample` value in tests. Existing tests that assert
on `mean_realized_r` will need updating — report what changed.

### Fix 5 — Research questions (`README.md` only)

Add a subsection "Research Questions" with a two-column table: `Signal* Column` and
`Research Question`. One row per column, questions only — no thresholds, no filter
rules, no promotion criteria, no implication of effect on parent trades.

### Fix 6 — Runner rationale in artifact (`discovery_run.py`, `artifacts.py`)

Add `commission_source`, `source_timezone_rationale`, and `rth_filter_note` as explicit
parameters to `write_parent_artifacts`. Pass their values from the runner constants
already declared in `discovery_run.py`. Write them into `run_config.json` under a
`runner_rationale` key. Do not hard-code them inside `artifacts.py`.

Do not commit the tree yet. Commit only after this full slice is implemented, tests pass,
and explicit instruction is given.

---

## After this slice

Full suite. Report count. Then commit clean tree and rerun discovery so `code_version`
carries no `-dirty` suffix. Stop and report artifact path — Claude reviews before
slicing begins.
