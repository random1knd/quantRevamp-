# Claude → Codex (2026-05-29): refresh stale docs after the engine commit (a259792, pushed)

Docs-only pass. No code, no strategy logic, no final-test data. The engine work is
committed and pushed (`a259792`). After this, write `codexArg`; the user reviews, then
decides the commit. Flag any other stale doc you notice while in these files.

## 1. HANDOFF.md — refresh to current state (PRIMARY)
- Title/date → 2026-05-29.
- Resume commit pointer → `a259792` on `origin/master` (the engine work is now
  committed AND pushed, not "uncommitted review-loop changes").
- Remove the stale "review loop is paused / Codex budget exhausted" line — the loop
  is active again today.
- Make the top "Session End" summary consistent with the Bucket A/B section: state
  that BOTH cold engines (block bootstrap + block permutation) are BUILT, engine-only,
  and unwired, and that the buildable-without-a-positive-edge work is COMPLETE.
- Refresh the test count to `364 passed`.
- Keep it a snapshot; the durable deferred record lives in
  `docs/overfitting_tests/README.md` and the overfitting specs.

## 2. docs/inventories/validation_tests.md — fix the stale Status column + add the engines
- The Status column is wrong: items marked "To build" / "Later" are built. Update to
  reflect reality (coverage-only on the rejected child): realized-R, min-count, MC
  centered bootstrap, MC equity curves, walk-forward, threshold nudge/neighborhood,
  cross-instrument, market-data permutation = BUILT (coverage-only). Deflated Sharpe =
  UNAVAILABLE (needs the slicer to persist a Sharpe/score distribution), not "later".
- Add the two dependence-aware engines as BUILT, engine-only, UNWIRED, and flag them as
  the promotion-grade gates vs the i.i.d. diagnostics:
  - block bootstrap (`shared/validation/block_bootstrap.py`)
  - block permutation (`shared/validation/block_permutation.py`)
- Keep the honesty markers: built ≠ wired; these are coverage/diagnostic until a real
  positive candidate + reviewed wiring.

## 3. docs/repo_structure.md — OPTIONAL / low priority
It is a folder-level "intended shape" sketch with placeholder strategy names
(`kalman_vwap_fade`, `absorption_fade`). Either refresh the example to the real
`vwap_zscore_fade` or leave it as an explicit sketch. Your call; do not over-invest.

## After you finish
- Run the full suite (docs-only, should stay `364 passed`).
- Report in `codexArg`; do not commit — the user reviews and decides the commit.
